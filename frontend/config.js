window.QR_APP_CONFIG = Object.assign(
    {
        backendUrl: window.location.origin,
        frontendUrl: window.location.origin,
        defaultLanguage: "uz"
    },
    window.QR_APP_CONFIG || {}
);
